# PRD V1.3 Demo 执行检查

检查对象：用户指定的 V1.3 PRD 与本目录当前页面。所有“通过”均指前端模拟评审，不代表真实生产验证。

## 已完成并验证的交互

| 评审项 | 结果 | 可观察的变化 |
| --- | --- | --- |
| 三种构建入口 | 已实现 | 简单配置、基础编排、受控服务创建；任务执行方式独立选择 |
| 空白创建与建图 | 已实现、规则测试通过 | 输入/模型/输出/异常节点可添加连接；断线被静态检查拦截 |
| 简单配置转流程 | 已实现 | 保留模型、提示词、知识、会话和发布参数，进入可编辑流程 |
| 节点与变量映射 | 已补齐、测试通过 | 映射实际影响节点收到的输入；无来源、非上游、无字段被拦截 |
| 条件与异常路径 | 已实现、浏览器验证无结果路径 | 未执行节点不显示虚构输出，工具超时有有限重试和兜底 |
| 静态检查 | 已实现 | 必填、类型、入口出口、连线、可达性、分支、循环、异常出口与依赖 |
| 单节点与完整调试 | 已实现 | 输入输出、引用、耗时、错误、Skill 与实际路径记录 |
| 候选与生产隔离 | 已实现、测试通过 | 保存草稿清除当前候选关联；固化深复制，生产不被覆盖 |
| 版本依赖 | 已实现 | 固定模型/配置/节点/知识/政策/工具/模板/Skill，历史快照可查看 |
| 历史评测 | 已补齐、测试通过 | 重新评测保留旧报告；更新规则后旧报告不满足当前发布门禁 |
| B 质量与审批拦截 | 已实现、测试通过 | 红线失败不能发布；编辑、审批、发布角色分离 |
| A 灰度与恢复 | 已实现、测试通过 | 稳定用户分桶、10%/50%/全量、模拟部署失败不改生产、回退恢复依赖 |
| 点击/采纳回传 | 已补齐、测试通过 | 关联曝光、实验、版本；重复 eventId 去重；效果率按当前模拟事件计算 |
| 批任务与重试 | 已实现、测试通过 | 3 成功/2 失败，重试只处理失败项，重复批次返回原任务 |
| 批条目统一追踪 | 已补齐、测试通过 | 每条已执行内容关联任务、版本、政策和运行记录，重试保留历史 |
| 政策失效保护 | 已实现、测试通过 | 暂停未完成批项，禁止新调用与恢复，支持转人工 |
| 新知识快照 | 已补齐、测试通过 | 新导入的文本实际参与模拟回答，不再硬编码仅识别 kb@2 |
| 多轮版本绑定 | 已实现、测试通过 | 回退影响新会话；旧会话维持原版本；失效依赖仍会阻断 |
| 模板与 Skill | 已实现 | 模板保存复用，Skill 发布新版本后显式绑定，原依赖不自动更新 |
| 受控服务边界 | 已实现、测试通过 | 契约注册、模拟测试与 externalTraceId；没有虚构内部节点 |
| 本地状态与重置 | 已实现 | 刷新保留评审状态；重置前确认 |

浏览器已检查新版工作台、流程画布、静态检查、无结果实际路径、固化与评测结果。自动检查覆盖 A/B/C 关键状态和 30 个应用/页面组合，以及弹窗按钮事件语法。

## 本次发现并修复的缺口

1. 原变量映射仅显示配置，现在实际解析上游输出和输入契约字段。
2. 新导入知识原先仍会得到固定旧答案，现在读取绑定快照文本。
3. 批任务原先仅在任务表展示，现在可跳到统一运行追踪。
4. 效果页原先只有固定比较表，现在可回传点击/采纳并真实更新模拟计数。
5. 重评原先覆盖同版本旧报告，现在保留历史评测列表。
6. 更新为新版页面入口与检查脚本，移除当前发布目录中的旧版脚本。

## 尚未执行或不应声称完成的部分

- 真实模型、向量检索、订单/社区接口、外部代码服务均未连接，当前为确定性 Mock。
- 高并发、批量吞吐、真实限流队列、扩缩容、成本与时延并未实测；看板对应项为示例。
- 真实身份认证、公司级空间隔离、密钥托管、后端持久化及真实审批通知未实现；角色仅为页面演示。
- 评测数据采用固定模拟指标，不会对任意导入语料执行真实模型评分；问题样本进入下一版数据集草稿。
- 没有 redcowork 在线提交链接；当前交付本地运行页面和源文件包。
- 循环、并行、子流程、多 Agent、自主 Skill 调度、自动放量和任意代码托管属于 PRD 二期，不纳入本 Demo。

可执行检查：`node verify.cjs`。无需安装依赖。

## 画布交互补充验收
- 拖动节点：坐标自由调整，连线实时跟随；刷新保留布局，草稿修订和评测结果不变。
- 输出端口拖到输入端口：生成有向连线；支持点选两端口连接。条件节点提供是/否出口，异常出口以虚线连接人工/保底节点。
- 点击连线后删除，或按 Delete；修改连线后提示保存并重新检查。
- 拖动空白平移，滚轮或 +/- 缩放；支持适应视图、100% 与展开画布。
- 拦截自身连接、重复出口、循环和错误异常目标。
- 自动检查：node verify.cjs（含 verify-canvas.cjs）。浏览器已实测节点拖动、端口拖线、删除重连、重复拦截、平移与滚轮缩放。
